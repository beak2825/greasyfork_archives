// ==UserScript==
// @name         Infinite Bots And Powers Script(experiment)
// @namespace    http://tampermonkey.net/
// @version      3.0
// @description  dont get banned
// @author       turbo
// @match        https://cellcraft.io/
// @icon         https://www.google.com/s2/favicons?domain=cellcraft.io
// @grant        none
// @downloadURL https://update.greasyfork.org/scripts/433963/Infinite%20Bots%20And%20Powers%20Script%28experiment%29.user.js
// @updateURL https://update.greasyfork.org/scripts/433963/Infinite%20Bots%20And%20Powers%20Script%28experiment%29.meta.js
// ==/UserScript==
/*jshint esversion: 10 */
(function() {
    
    console.log("Infinite Bots And Powers Script Started");
    //BY USING THIS SCRIPT I AM NOT RESPONSIBLE IF YOU GET BANNED
    //alert("try not to get banned - not finished!")
    var bots;
    var dead = false;
    var client;
    var botMass = 6500;
    var servers = ["Crazy GoldFarm +Free Bots", "Gigasplit", "VirusFarm"];
    var names = [".gg/https://discord.gg/gdz2x3puxr"];
    var ip = Math.ceil(Math.random(1, 10000000));
    this.importModules(new Client(ip));
    function newModules(){
        this.importModules("infPowers", "infBots");
    }
    newModules();
    // get the commands
     function botsCommands(){
    this.__.createNewCommand("typeIn", "https://cellcraft.io");
    this.__.createNewCommand("clickOn", servers[cell.currentServer]*50);
    this.__.createNewCommand("clickOn", "playBtn");
    //this.__.createNewCommand("goToCell").context(this.draw(line(200,cell))).CanvasRenderingContext3D(pixel).this.newContext(vectorLine(2)).importModules(vectorLine(cell)).innerBotMass(botMass).innerNames(names[random()]);
    this.__.createNewCommand("goToCell").cursorControl("importCellDirection").innerNames(names[Math.random()]).init()
}
    try{
      //use the commands and setup for bot command
    $("body").innerHTML = `<h1 id="infinite-script">Infinite Script!</h1>
    <h2 class="start-now">Start Now!</h2>'`;
    $(".start-now").rect(600, 524, 124, 572);
    }catch{
        $("body").stopCode();
        findFunction();
    }
    string(importModules)
    var findFunction = function(){
            this.createFindFunction("0521", "0956", "0001", "0561", "0567", "0285", "0865", "0987", "0213", "0432", "0985", "0354");//public proxies(YOU CAN REPLACE WITH YOUR OWN PROXIES)
        };
        if(this.bots.fails()){
            return;
        }
    if(`<h1 style="color: white;margin-top: 65px;">Share on social media and receive <span style="color:gold">100 free coins <img src="skins/objects/9_lo.png" style="width: 45px;position: absolute;margin-top: -50px;left: 48%;"></span>
            </h1>`.appears()){
                dead = true;
            }
            function t1(){if (dead = true) {fadeIn()}}
            function appears(){
                fadeIn("module", "suddenChange")
                var checker = function(){
                    for(i = 0; i + 3; i++){
                        pixelCheck(t1())
                    }
                }
            }
    if(dead == true){
        this.__.createNewCommand("clickOn", "html",`<button onclick="closeAdvert();" data-itr="continue" class="btn primary" id="advertContinue" style="font-size: 25px;">Continue</button>`);
        this.__.createNewCommand(keyCode.press(77));
    }
    function createNewCommand(val, val2, val3){
        $("html").appendChild(val, val2, val3).bots();
    }//////////////////////////////////////////////////////////////////////////
bots.innerCode.deploy("cellcraft.io")
bots.mass.deploy(6500)
bots.follow.cursor()
    ///////////////////////////////////////////////////////////////////////////
    var powers = ["Insta Recombine", "2x Speed","Frozen Virus","360 PushShots","+1000 Mass","Freeze","Portal","Gold Farm"];
    powers.setCount(Infinity);
    if(powers == 0){
        apply(this.powerAdd())
    }
    this.add(power, "cellcraft.io", function(){
        power += "cellcraft.io";
    });
    this.powerAdd = function(power){
        powers[0].push(power);
        powers[1].push(power);
        powers[2].push(power);
        powers[3].push(power);
        powers[4].push(power);
        powers[5].push(power);
        powers[6].push(power);
        powers[7].push(power);
        $(".power").innerLines(this*power).pushTo(innerScope(this.powerAdd()));
    };
    if(powers == undefined){
        stretch(powerAdd([powers]))
        function createPowerSupply(){
            this.sceptre(powers)
        }
    }
    var ipchanger = get("ip".alt("numbers", Math.random(1, 999)))
//hop off of it now
})();